class newUser {
  constructor(nameAccount, curentSold, curentIban) {
    (this.nameAccount = nameAccount),
      (this.curentSold = curentSold),
      (this.curentIban = curentIban);
  }
};

export default newUser;